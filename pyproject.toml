[tool.black]
line-length = 120
target-version = ['py37']

[tool.isort]
profile = 'black'
line_length = 120
lines_between_sections=0

[tool.mypy]
ignore_missing_imports = true

[tool.robotidy]
configure = [
    "ReplaceReturns:enabled=False"
]
